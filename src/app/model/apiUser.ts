export interface ApiUser {
  readonly id: string;
  firebaseToken : string;

  // status: number;  //1 for coach, 2 for coachee
  // email : string;
  // display_name: string;
  // avatar_url: string;
  // score: number;
  // start_date : string;
  //
  //
}
